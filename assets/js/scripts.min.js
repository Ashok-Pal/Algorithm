/*!
 * fireshell
 * Fiercely quick and opinionated front-ends
 * http://getfireshell.com
 * @author Todd Motto
 * @version 1.0.0
 * Copyright 2013. MIT licensed.
 */
(function($, window, document, undefined) {

    $(function() {
        // FireShell

        /*-----------------header animation----------------------*/

        if ($(window).width() > 1005) {
            $(window).scroll(function() {
                var scroll = $(window).scrollTop();
                if (scroll >= 40) {
                    $("nav.navbar").addClass("header-animate");
                } else {
                    $("nav.navbar").removeClass("header-animate");
                }
            });
        }


        /*--------------product height-----------------------*/
        /*	if($(window).width() > 768){
        		var equalheight = function(container){
        		var currentTallest = 0,
        		     currentRowStart = 0,
        		     rowDivs = new Array(),
        		     $el,
        		     topPosition = 0;
        		 $(container).each(function() {

        		   $el = $(this);
        		   $($el).height('auto')
        		   topPostion = $el.position().top;

        		   if (currentRowStart != topPostion) {
        		     for (currentDiv = 0 ; currentDiv < rowDivs.length ; currentDiv++) {
        		       rowDivs[currentDiv].height(currentTallest);
        		     }
        		     rowDivs.length = 0; // empty the array
        		     currentRowStart = topPostion;
        		     currentTallest = $el.height();
        		     rowDivs.push($el);
        		   } else {
        		     rowDivs.push($el);
        		     currentTallest = (currentTallest < $el.height()) ? ($el.height()) : (currentTallest);
        		  }
        		   for (currentDiv = 0 ; currentDiv < rowDivs.length ; currentDiv++) {
        		     rowDivs[currentDiv].height(currentTallest);
        		   }
        		 });
        		}



        		$(window).load(function() {
        		  equalheight('.product-items');
        		    var thisht = 0; 
        		    $('.product-items').each(function(){        
        		            thisht = $(this).height() - 50;  
        		         $(this).find('.item').height(thisht);
        		    });

        		});

        	}*/

        /*-----------------same page linking smooth scroll-------------------*/

        // // Select all links with hashes
        // $('a[href*="#"]')
        //   // Remove links that don't actually link to anything
        //   .not('[href="#"]')
        //   .not('[href="#0"]')
        //   .click(function(event) {
        //     // On-page links
        //     if (
        //       location.pathname.replace(/^\//, '') == this.pathname.replace(/^\//, '') 
        //       && 
        //       location.hostname == this.hostname
        //     ) {
        //       // Figure out element to scroll to
        //       var target = $(this.hash);
        //       target = target.length ? target : $('[name=' + this.hash.slice(1) + ']');
        //       // Does a scroll target exist?
        //       if (target.length) {
        //         // Only prevent default if animation is actually gonna happen
        //         event.preventDefault();
        //         $('html, body').animate({
        //           scrollTop: target.offset().top - 100
        //         }, 1000, function() {
        //           // Callback after animation
        //           // Must change focus!
        //           var $target = $(target);
        //           $target.focus();
        //           if ($target.is(":focus")) { // Checking if the target was focused
        //             return false;
        //           } else {
        //             $target.attr('tabindex','-1'); // Adding tabindex for elements not focusable
        //             $target.focus(); // Set focus again
        //           };
        //         });
        //       }
        //     }
        //   });

        /*--------------sliders-----------------------*/
        $('.slider,.slider1').slick({
            dots: true,
            infinite: false,
            speed: 300,
            slidesToShow: 1,
            slidesToScroll: 1,
            responsive: [{
                    breakpoint: 1024,
                    settings: {
                        slidesToShow: 1,
                        slidesToScroll: 1,
                        infinite: true,
                        dots: true
                    }
                },
                {
                    breakpoint: 600,
                    settings: {
                        slidesToShow: 1,
                        slidesToScroll: 1
                    }
                },
                {
                    breakpoint: 480,
                    settings: {
                        slidesToShow: 1,
                        slidesToScroll: 1
                    }
                }
                // You can unslick at a given breakpoint now by adding:
                // settings: "unslick"
                // instead of a settings object
            ]
        });

        /*----------------------product detail page----------------------------*/

        $('.logo-carousel').slick({
            dots: true,
            infinite: false,
            speed: 300,
            slidesToShow: 4,
            slidesToScroll: 4,
            responsive: [{
                    breakpoint: 1024,
                    settings: {
                        slidesToShow: 3,
                        slidesToScroll: 3,
                        infinite: true,
                        dots: true
                    }
                },
                {
                    breakpoint: 600,
                    settings: {
                        slidesToShow: 1,
                        slidesToScroll: 1
                    }
                },
                {
                    breakpoint: 480,
                    settings: {
                        slidesToShow: 1,
                        slidesToScroll: 1
                    }
                }
                // You can unslick at a given breakpoint now by adding:
                // settings: "unslick"
                // instead of a settings object
            ]
        });

        /*---------------------------------------------*/

        $('.outer-carousel').slick({
            dots: true,
            infinite: false,
            speed: 300,
            slidesToShow: 4,
            slidesToScroll: 4,
            responsive: [{
                    breakpoint: 1025,
                    settings: {
                        slidesToShow: 3,
                        slidesToScroll: 3,
                        infinite: true,
                        dots: true
                    }
                },
                {
                    breakpoint: 768,
                    settings: {
                        slidesToShow: 2,
                        slidesToScroll: 2
                    }
                },
                {
                    breakpoint: 767,
                    settings: {
                        slidesToShow: 1,
                        slidesToScroll: 1
                    }
                }
                // You can unslick at a given breakpoint now by adding:
                // settings: "unslick"
                // instead of a settings object
            ]
        });

    });

    $(window).on('load', function() {
        /*--------------user height-----------------------*/
        if ($(window).width() > 768) {
            /*$(".users-listing .inner").removeAttr('style');*/
            var height = $(".users-listing").height() - 200;
            $(".users-listing .inner").height(height);
        }
        $(window).resize(function() {
            if ($(window).width() > 768) {
                /*$(".users-listing .inner").removeAttr('style');*/
                var height = $(".users-listing").height() - 200;
                $(".users-listing .inner").height(height);
            }
        });

        $(window).trigger('resize');
    });

    // var video = document.getElementById('bgvid');
    //    video.volume = .05;

    /* var video = document.getElementById('bgvid'); 
	 video.addEventListener('click',function(){ 
	 	video.play(); 
	 },false);
	 $(window).on('load',function(){
	 	setTimeout(function(){
	 	$('#bgvid')[0].trigger('click');
	 	alert('aaya');
	 	},100);	
	 });
*/


})(jQuery, window, document);